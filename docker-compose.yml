services:
  postgres:
    image: postgres:16-alpine
    container_name: fs0005-postgres
    restart: always
    env_file:
      - ./backend/.env
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
